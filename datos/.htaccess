# Fallback data folder — only used if the outside-public_html path isn't
# writable. Never served to the browser under any circumstance.
<IfModule mod_authz_core.c>
  Require all denied
</IfModule>
<IfModule !mod_authz_core.c>
  Order deny,allow
  Deny from all
</IfModule>
